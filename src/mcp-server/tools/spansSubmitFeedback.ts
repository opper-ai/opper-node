/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { spansSubmitFeedback } from "../../funcs/spansSubmitFeedback.js";
import * as models from "../../models/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  spanId: z.string(),
  submitFeedbackRequest: models.SubmitFeedbackRequest$inboundSchema,
};

export const tool$spansSubmitFeedback: ToolDefinition<typeof args> = {
  name: "spans-submit-feedback",
  description: `Submit Span Feedback

Submit human feedback for a span.

This endpoint allows you to provide feedback (thumbs up/down) on a span's output.
The feedback is stored on the associated generation and can trigger auto-save
to the function's dataset based on the observer's configuration.

- score=1.0: Positive feedback (thumbs up)
- score=0.0: Negative feedback (thumbs down)
- Intermediate values (e.g., 0.5) are supported for nuanced feedback`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await spansSubmitFeedback(
      client,
      args.spanId,
      args.submitFeedbackRequest,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
