/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { languageModelsDeleteAlias } from "../../funcs/languageModelsDeleteAlias.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  aliasId: z.string(),
};

export const tool$languageModelsDeleteAlias: ToolDefinition<typeof args> = {
  name: "language-models-delete-alias",
  description: `Delete Model Alias

Delete a model alias.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await languageModelsDeleteAlias(
      client,
      args.aliasId,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
