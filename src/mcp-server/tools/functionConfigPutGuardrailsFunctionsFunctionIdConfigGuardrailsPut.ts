/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { functionConfigPutGuardrailsFunctionsFunctionIdConfigGuardrailsPut } from "../../funcs/functionConfigPutGuardrailsFunctionsFunctionIdConfigGuardrailsPut.js";
import * as models from "../../models/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  functionId: z.string(),
  requestBody: z.array(models.GuardrailItem$inboundSchema),
};

export const tool$functionConfigPutGuardrailsFunctionsFunctionIdConfigGuardrailsPut:
  ToolDefinition<typeof args> = {
    name:
      "function-config-put-guardrails-functions-function-id-config-guardrails-put",
    description: `Put Guardrails

Update the project-level guardrails configuration.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] =
        await functionConfigPutGuardrailsFunctionsFunctionIdConfigGuardrailsPut(
          client,
          args.functionId,
          args.requestBody,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
