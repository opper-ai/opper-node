/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { languageModelsGetAliasByName } from "../../funcs/languageModelsGetAliasByName.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  name: z.string(),
};

export const tool$languageModelsGetAliasByName: ToolDefinition<typeof args> = {
  name: "language-models-get-alias-by-name",
  description: `Get Model Alias By Name

Get a model alias by its name.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await languageModelsGetAliasByName(
      client,
      args.name,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
