/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { rerankListModels } from "../../funcs/rerankListModels.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$rerankListModels: ToolDefinition = {
  name: "rerank-list-models",
  description: `List Rerank Models

List all available reranking models.

Returns a list of all reranking models available on the Opper platform,
including their hosting providers, locations, and pricing information.`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await rerankListModels(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
