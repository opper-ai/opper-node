/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { modelsDeleteModelAliasModelsAliasesAliasIdDelete } from "../../funcs/modelsDeleteModelAliasModelsAliasesAliasIdDelete.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  aliasId: z.string(),
};

export const tool$modelsDeleteModelAliasModelsAliasesAliasIdDelete:
  ToolDefinition<typeof args> = {
    name: "models-delete-model-alias-models-aliases-alias-id-delete",
    description: `Delete Model Alias

Delete a model alias.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] =
        await modelsDeleteModelAliasModelsAliasesAliasIdDelete(
          client,
          args.aliasId,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      return formatResult(void 0, apiCall);
    },
  };
