/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { functionConfigGetFunctionConfigFunctionsFunctionIdConfigGet } from "../../funcs/functionConfigGetFunctionConfigFunctionsFunctionIdConfigGet.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  functionId: z.string(),
};

export const tool$functionConfigGetFunctionConfigFunctionsFunctionIdConfigGet:
  ToolDefinition<typeof args> = {
    name:
      "function-config-get-function-config-functions-function-id-config-get",
    description: `Get Function Config

Return the merged function configuration (project-level + per-function).`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] =
        await functionConfigGetFunctionConfigFunctionsFunctionIdConfigGet(
          client,
          args.functionId,
          { fetchOptions: { signal: ctx.signal } },
        ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
