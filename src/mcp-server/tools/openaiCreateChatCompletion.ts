/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { openaiCreateChatCompletion } from "../../funcs/openaiCreateChatCompletion.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.Payload$inboundSchema,
};

export const tool$openaiCreateChatCompletion: ToolDefinition<typeof args> = {
  name: "openai-create-chat-completion",
  description: `Chat Completions`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await openaiCreateChatCompletion(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
