/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { languageModelsDeleteCustom } from "../../funcs/languageModelsDeleteCustom.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  modelId: z.string(),
};

export const tool$languageModelsDeleteCustom: ToolDefinition<typeof args> = {
  name: "language-models-delete-custom",
  description: `Delete Custom Model

Delete a custom language model.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await languageModelsDeleteCustom(
      client,
      args.modelId,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
