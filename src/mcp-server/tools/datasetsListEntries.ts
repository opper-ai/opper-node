/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { datasetsListEntries } from "../../funcs/datasetsListEntries.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  datasetId: z.string(),
  offset: z.number().int().default(0),
  limit: z.number().int().default(100),
};

export const tool$datasetsListEntries: ToolDefinition<typeof args> = {
  name: "datasets-list-entries",
  description: `List Dataset Entries`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await datasetsListEntries(
      client,
      args.datasetId,
      args.offset,
      args.limit,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
