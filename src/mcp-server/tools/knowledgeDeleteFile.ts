/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { knowledgeDeleteFile } from "../../funcs/knowledgeDeleteFile.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  knowledgeBaseId: z.string(),
  fileId: z.string(),
};

export const tool$knowledgeDeleteFile: ToolDefinition<typeof args> = {
  name: "knowledge-delete-file",
  description: `Delete File From Knowledge Base

Delete a file from a knowledge base by its id`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await knowledgeDeleteFile(
      client,
      args.knowledgeBaseId,
      args.fileId,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
