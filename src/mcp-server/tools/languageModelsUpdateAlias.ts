/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { languageModelsUpdateAlias } from "../../funcs/languageModelsUpdateAlias.js";
import * as models from "../../models/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  aliasId: z.string(),
  updateModelAliasRequest: models.UpdateModelAliasRequest$inboundSchema,
};

export const tool$languageModelsUpdateAlias: ToolDefinition<typeof args> = {
  name: "language-models-update-alias",
  description: `Update Model Alias

Update a model alias.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await languageModelsUpdateAlias(
      client,
      args.aliasId,
      args.updateModelAliasRequest,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
