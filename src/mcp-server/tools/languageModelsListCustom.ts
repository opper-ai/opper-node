/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { languageModelsListCustom } from "../../funcs/languageModelsListCustom.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  offset: z.number().int().default(0),
  limit: z.number().int().default(100),
};

export const tool$languageModelsListCustom: ToolDefinition<typeof args> = {
  name: "language-models-list-custom",
  description: `List Custom Models

List all custom language models for the organization that owns the API key.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await languageModelsListCustom(
      client,
      args.offset,
      args.limit,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
