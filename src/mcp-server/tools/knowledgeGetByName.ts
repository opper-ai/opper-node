/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { knowledgeGetByName } from "../../funcs/knowledgeGetByName.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  knowledgeBaseName: z.string(),
};

export const tool$knowledgeGetByName: ToolDefinition<typeof args> = {
  name: "knowledge-get-by-name",
  description: `Get Knowledge Base By Name

Get a knowledge base by its name`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await knowledgeGetByName(
      client,
      args.knowledgeBaseName,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
