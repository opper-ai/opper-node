/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { functionsGetByName } from "../../funcs/functionsGetByName.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  name: z.string(),
};

export const tool$functionsGetByName: ToolDefinition<typeof args> = {
  name: "functions-get-by-name",
  description: `Get Function By Name

Get the latest revision of a function by name`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await functionsGetByName(
      client,
      args.name,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
