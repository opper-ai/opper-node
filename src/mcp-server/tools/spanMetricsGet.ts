/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { spanMetricsGet } from "../../funcs/spanMetricsGet.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  spanId: z.string(),
  metricId: z.string(),
};

export const tool$spanMetricsGet: ToolDefinition<typeof args> = {
  name: "span-metrics-get",
  description: `Get Metric

Get a metric for a span`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await spanMetricsGet(
      client,
      args.spanId,
      args.metricId,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
