/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { languageModelsGetCustom } from "../../funcs/languageModelsGetCustom.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  modelId: z.string(),
};

export const tool$languageModelsGetCustom: ToolDefinition<typeof args> = {
  name: "language-models-get-custom",
  description: `Get Custom Model

Get a custom language model by its ID.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await languageModelsGetCustom(
      client,
      args.modelId,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
