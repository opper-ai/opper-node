/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { spanMetricsDelete } from "../../funcs/spanMetricsDelete.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  spanId: z.string(),
  metricId: z.string(),
};

export const tool$spanMetricsDelete: ToolDefinition<typeof args> = {
  name: "span-metrics-delete",
  description: `Delete Metric

Delete a metric for a span`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await spanMetricsDelete(
      client,
      args.spanId,
      args.metricId,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    return formatResult(void 0, apiCall);
  },
};
