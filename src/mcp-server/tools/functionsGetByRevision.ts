/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { functionsGetByRevision } from "../../funcs/functionsGetByRevision.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  functionId: z.string(),
  revisionId: z.string(),
};

export const tool$functionsGetByRevision: ToolDefinition<typeof args> = {
  name: "functions-get-by-revision",
  description: `Get Function By Revision

Get a function by ID with a specific revision`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await functionsGetByRevision(
      client,
      args.functionId,
      args.revisionId,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
