/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { languageModelsRegisterCustom } from "../../funcs/languageModelsRegisterCustom.js";
import * as models from "../../models/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: models.RegisterCustomModelRequest$inboundSchema,
};

export const tool$languageModelsRegisterCustom: ToolDefinition<typeof args> = {
  name: "language-models-register-custom",
  description: `Register Custom Model

Register a custom language model with the organization that owns the API key.`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await languageModelsRegisterCustom(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
