/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { functionsCreate } from "../../funcs/functionsCreate.js";
import * as models from "../../models/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: models.CreateFunctionRequest$inboundSchema,
};

export const tool$functionsCreate: ToolDefinition<typeof args> = {
  name: "functions-create",
  description: `Create Function

Create a function`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await functionsCreate(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
