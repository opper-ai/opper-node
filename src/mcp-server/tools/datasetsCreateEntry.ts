/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { datasetsCreateEntry } from "../../funcs/datasetsCreateEntry.js";
import * as models from "../../models/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  datasetId: z.string(),
  createDatasetEntryRequest: models.CreateDatasetEntryRequest$inboundSchema,
};

export const tool$datasetsCreateEntry: ToolDefinition<typeof args> = {
  name: "datasets-create-entry",
  description: `Create Dataset Entry`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await datasetsCreateEntry(
      client,
      args.datasetId,
      args.createDatasetEntryRequest,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
