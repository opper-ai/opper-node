/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod";
import { spanMetricsUpdateMetric } from "../../funcs/spanMetricsUpdateMetric.js";
import * as models from "../../models/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  spanId: z.string(),
  metricId: z.string(),
  updateSpanMetricRequest: models.UpdateSpanMetricRequest$inboundSchema,
};

export const tool$spanMetricsUpdateMetric: ToolDefinition<typeof args> = {
  name: "span-metrics-update-metric",
  description: `Update Metric

Update a metric for a span`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await spanMetricsUpdateMetric(
      client,
      args.spanId,
      args.metricId,
      args.updateSpanMetricRequest,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
