/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type GuardrailItem = {
  check: string;
  appliesTo?: string | undefined;
  action?: string | undefined;
};

/** @internal */
export const GuardrailItem$inboundSchema: z.ZodType<
  GuardrailItem,
  z.ZodTypeDef,
  unknown
> = z.object({
  check: z.string(),
  appliesTo: z.string().default("both"),
  action: z.string().default("flag"),
});
/** @internal */
export type GuardrailItem$Outbound = {
  check: string;
  appliesTo: string;
  action: string;
};

/** @internal */
export const GuardrailItem$outboundSchema: z.ZodType<
  GuardrailItem$Outbound,
  z.ZodTypeDef,
  GuardrailItem
> = z.object({
  check: z.string(),
  appliesTo: z.string().default("both"),
  action: z.string().default("flag"),
});

export function guardrailItemToJSON(guardrailItem: GuardrailItem): string {
  return JSON.stringify(GuardrailItem$outboundSchema.parse(guardrailItem));
}
export function guardrailItemFromJSON(
  jsonString: string,
): SafeParseResult<GuardrailItem, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GuardrailItem$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GuardrailItem' from JSON`,
  );
}
