/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

export type SubmitFeedbackRequest = {
  /**
   * Feedback score (0.0=negative, 1.0=positive)
   */
  score: number;
  /**
   * Optional comment explaining the feedback
   */
  comment?: string | null | undefined;
  /**
   * Force save to dataset (True=force save, False=never save, None=use auto-save config)
   */
  saveToDataset?: boolean | null | undefined;
};

/** @internal */
export const SubmitFeedbackRequest$inboundSchema: z.ZodType<
  SubmitFeedbackRequest,
  z.ZodTypeDef,
  unknown
> = z.object({
  score: z.number(),
  comment: z.nullable(z.string()).optional(),
  save_to_dataset: z.nullable(z.boolean()).optional(),
}).transform((v) => {
  return remap$(v, {
    "save_to_dataset": "saveToDataset",
  });
});
/** @internal */
export type SubmitFeedbackRequest$Outbound = {
  score: number;
  comment?: string | null | undefined;
  save_to_dataset?: boolean | null | undefined;
};

/** @internal */
export const SubmitFeedbackRequest$outboundSchema: z.ZodType<
  SubmitFeedbackRequest$Outbound,
  z.ZodTypeDef,
  SubmitFeedbackRequest
> = z.object({
  score: z.number(),
  comment: z.nullable(z.string()).optional(),
  saveToDataset: z.nullable(z.boolean()).optional(),
}).transform((v) => {
  return remap$(v, {
    saveToDataset: "save_to_dataset",
  });
});

export function submitFeedbackRequestToJSON(
  submitFeedbackRequest: SubmitFeedbackRequest,
): string {
  return JSON.stringify(
    SubmitFeedbackRequest$outboundSchema.parse(submitFeedbackRequest),
  );
}
export function submitFeedbackRequestFromJSON(
  jsonString: string,
): SafeParseResult<SubmitFeedbackRequest, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => SubmitFeedbackRequest$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'SubmitFeedbackRequest' from JSON`,
  );
}
