/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { remap as remap$ } from "../lib/primitives.js";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * Docling-specific OCR parameters for document parsing with layout analysis.
 */
export type DoclingOCRExtra = {
  /**
   * Output format: 'markdown' (default) returns clean text with tables, 'json' includes bounding boxes and full document structure.
   */
  outputFormat?: string | null | undefined;
  /**
   * OCR engine: null (auto-select, typically RapidOCR), 'easyocr' (good multilingual support), or 'tesseract' (fast, many languages).
   */
  ocrEngine?: string | null | undefined;
  /**
   * OCR language(s) to recognise, as ISO 639-1 two-letter codes (e.g. 'sv', 'en', 'de', 'da', 'no', 'fi', 'is'). Opper maps these to the chosen engine internally, so the same codes work for any engine. Requires ocr_engine 'tesseract' or 'easyocr' (the auto/RapidOCR engine ignores it). Pass several for mixed-language documents. Note: 'fi' (Finnish) is only available with 'tesseract'. Defaults to the engine's own default (English) when omitted.
   */
  lang?: Array<string> | null | undefined;
  /**
   * Force OCR on all pages even if text is extractable. Useful for scanned PDFs with embedded but incorrect text layers.
   */
  forceOcr?: boolean | null | undefined;
  /**
   * Table extraction mode: 'fast' (default) for speed, 'accurate' for complex tables with merged cells or nested structures.
   */
  tableMode?: string | null | undefined;
  /**
   * Scale factor for extracted images. Default is 1.0. Use 2.0 or higher for better resolution (slower processing).
   */
  imagesScale?: number | null | undefined;
  /**
   * Enable mathematical formula detection and LaTeX extraction. Equations are converted to LaTeX notation in the output.
   */
  doFormulaEnrichment?: boolean | null | undefined;
  /**
   * Enable code block detection and formatting. Code snippets are wrapped in markdown code fences with language detection.
   */
  doCodeEnrichment?: boolean | null | undefined;
};

/** @internal */
export const DoclingOCRExtra$inboundSchema: z.ZodType<
  DoclingOCRExtra,
  z.ZodTypeDef,
  unknown
> = z.object({
  output_format: z.nullable(z.string()).optional(),
  ocr_engine: z.nullable(z.string()).optional(),
  lang: z.nullable(z.array(z.string())).optional(),
  force_ocr: z.nullable(z.boolean()).optional(),
  table_mode: z.nullable(z.string()).optional(),
  images_scale: z.nullable(z.number()).optional(),
  do_formula_enrichment: z.nullable(z.boolean()).optional(),
  do_code_enrichment: z.nullable(z.boolean()).optional(),
}).transform((v) => {
  return remap$(v, {
    "output_format": "outputFormat",
    "ocr_engine": "ocrEngine",
    "force_ocr": "forceOcr",
    "table_mode": "tableMode",
    "images_scale": "imagesScale",
    "do_formula_enrichment": "doFormulaEnrichment",
    "do_code_enrichment": "doCodeEnrichment",
  });
});
/** @internal */
export type DoclingOCRExtra$Outbound = {
  output_format?: string | null | undefined;
  ocr_engine?: string | null | undefined;
  lang?: Array<string> | null | undefined;
  force_ocr?: boolean | null | undefined;
  table_mode?: string | null | undefined;
  images_scale?: number | null | undefined;
  do_formula_enrichment?: boolean | null | undefined;
  do_code_enrichment?: boolean | null | undefined;
};

/** @internal */
export const DoclingOCRExtra$outboundSchema: z.ZodType<
  DoclingOCRExtra$Outbound,
  z.ZodTypeDef,
  DoclingOCRExtra
> = z.object({
  outputFormat: z.nullable(z.string()).optional(),
  ocrEngine: z.nullable(z.string()).optional(),
  lang: z.nullable(z.array(z.string())).optional(),
  forceOcr: z.nullable(z.boolean()).optional(),
  tableMode: z.nullable(z.string()).optional(),
  imagesScale: z.nullable(z.number()).optional(),
  doFormulaEnrichment: z.nullable(z.boolean()).optional(),
  doCodeEnrichment: z.nullable(z.boolean()).optional(),
}).transform((v) => {
  return remap$(v, {
    outputFormat: "output_format",
    ocrEngine: "ocr_engine",
    forceOcr: "force_ocr",
    tableMode: "table_mode",
    imagesScale: "images_scale",
    doFormulaEnrichment: "do_formula_enrichment",
    doCodeEnrichment: "do_code_enrichment",
  });
});

export function doclingOCRExtraToJSON(
  doclingOCRExtra: DoclingOCRExtra,
): string {
  return JSON.stringify(DoclingOCRExtra$outboundSchema.parse(doclingOCRExtra));
}
export function doclingOCRExtraFromJSON(
  jsonString: string,
): SafeParseResult<DoclingOCRExtra, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => DoclingOCRExtra$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'DoclingOCRExtra' from JSON`,
  );
}
